#main.container
  %h3.header Edit a pending campaign

  =form_for @campaign, :method => :patch do |f|
    =render 'form', f: f
    =f.submit "Submit", :class => "btn btn-primary"
    =link_to "Cancel", campaign_change_path(@campaign), :class => "btn btn-secondary"
