%h1.display-4 Admin Dashboard

.row
  - @cards.each do |card|
    .col-lg-3.col-md-6.col-sm-12.card-set{ :style => "width: initial;"}
      %div{ :class => "card text-white mb-3 bg-#{card[2]}" }
        .card-body
          %h2.card-title= card[0]
          %p.card-text= card[1]


/ FORM FOR STATUS UPDATE **DOESNT WORK YET**

/ =form_for @status_update, :method => :post do |f|
/   .form-group.row
/     =f.label :status_time, "Time", :class => "col-sm-2 col-form-label"
/     .col-sm-10
/       =f.datetime_field :status_time, :class => "form_control"

/   .form-group.row
/     =f.label :status_text, "Text", :class => "col-sm-2 col-form-label"
/     .col-sm-10
/       =f.text_area :status_text, size: "100x3", :placeholder => "Enter your status update here", :class => "form-control"

/   .form-group.row
/     =f.label :status_campaign, "Campaign", :class => "col=sm-2 col-form-label"
/     .col-sm-10
/       =f.select :status_campaign, :class => "form-control"
/       -@campaigns.each do |campaign|
/         %option= campaign.title

/  =f.submit "Submit", :class => "btn btn-primary"


%h3 Things to implement:
%ul
  %li Organization Approval
  %li Campaign Approval
  %li Status Updates
  %li Organization/Campaign Overview - including is_featured/is_active toggles
  %li Payment Overview
  %li Adding Konosioni Donation
  %li Short Questions


=link_to "Database", rails_admin_path

%h3 Pending Campaigns and Campaign Changes
%table{:class => "table"}
  %thead
    %tr
      %th{:scope=>"col"} Name
      %th{:scope=>"col"} Request
      %th{:scope=>"col"} Approve
  %tbody
  - @campaign_changes.each do |campaign_change|
    %tr
      %td=link_to campaign_change.name, campaign_change_path(campaign_change.id), :target => "_blank"
      %td= campaign_change.action
      %td=link_to "Approve", admin_campaign_approval_path(:campaign_change_id => campaign_change.id), :class => "btn btn-primary"
