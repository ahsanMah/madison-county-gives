%h3.header Edit Organization Profile

=form_for @organization, :multipart => true, :method => :patch do |f|
  =render 'form', f: f

  =f.submit "Submit", :class => "btn btn-primary"
  =link_to "Cancel", organization_path(@organization), :class => "btn btn-secondary"
